# Model-Based Grading — Streamlit

Quick Streamlit UI for the model-based grading notebook.

Run locally:

```
pip install -r requirements.txt
streamlit run streamlit_app.py
```

- Place or generate `dataset.json` in this folder.
- The app will call the configured Anthropic/Claude model using env vars.
